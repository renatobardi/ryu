import * as React from 'react';

/** Chave da issue (`RYU-142`) — 11px mono, zinc-500. Único lugar da UI com fonte mono junto de cron e paths. */
export interface IssueKeyProps { children?: React.ReactNode; style?: React.CSSProperties }
export declare function IssueKey(props: IssueKeyProps): JSX.Element;

Prioridade da issue. `none` não renderiza nada — é assim no template do board.

```jsx
<PriorityTag priority="urgent" />
```

Vermelho → laranja → amarelo → zinc. Diferente das StatusPill, o raio aqui é 4px.

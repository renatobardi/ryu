Estado vazio. A cópia do Ryu é curta e direta, sempre em pt-BR, às vezes apontando o próximo passo.

```jsx
<EmptyState>Nenhum agente ainda. Crie o primeiro em <a href="#">Agents</a>.</EmptyState>
```

Em listas simples (Skills, Squads) o produto usa só uma linha de texto zinc-500 sem card — passe `dashed={false}` ou renderize texto puro.

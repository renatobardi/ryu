Input de texto do Ryu — sem label embutido; use um `<label>` de 12px zinc-500 acima quando precisar.

```jsx
<Input placeholder="Nova issue..." />
<Input size="sm" placeholder="Search  ⌘K" />
<Input tone="sunken" placeholder="Nome do autopilot" />
```

O único feedback de foco é a borda virar violet-500 — não há ring nem sombra.

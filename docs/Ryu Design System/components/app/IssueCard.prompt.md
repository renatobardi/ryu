Card de issue do board — `draggable`, cursor `grab`, título vira violet-300 no hover.

```jsx
<IssueCard issueKey="RYU-142" title="Corrigir bug no runner" priority="urgent" assignee="codebot" />
```

O assignee aparece como chip cinza com 🤖 (agente) ou 👤 (humano).

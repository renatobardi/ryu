Barra superior do workspace — quase vazia por design: só o estado do realtime e o Inbox.

```jsx
<TopBar connection="connected" inboxCount={3} />
```

O ponto ● é o indicador de WebSocket: emerald quando aberto, red no `onclose`.

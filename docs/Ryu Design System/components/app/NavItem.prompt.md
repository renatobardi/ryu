Item de navegação da sidebar. Os rótulos são em **inglês** (Inbox, Board, Agents) mesmo com o resto da UI em pt-BR.

```jsx
<NavItem icon="📥" label="Inbox" count={3} />
<NavItem icon="▦" label="Issues" active />
```

Ativo = `zinc-800/80` + texto zinc-100. Hover = `zinc-800/50`.
